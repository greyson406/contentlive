# 数据迁移配置操作手册

> 适用页面：迁移配置 / 迁移任务明细
> 适用对象：后台管理员 / 运维
> 阅读方式：本手册说明一键数据迁移（全量 + 增量）的完整操作流程，包括数据源配置、连接测试与迁移执行。

---

## 一、功能概述

数据迁移功能用于将旧版系统的数据平滑迁移至新平台，支持全量 + 增量两种迁移模式。系统内置 Upsert（插入或更新）与防重复跳过机制，因此迁移任务可随时重复发起——引擎会自动跳过已存在的数据，仅对增量数据或变动字段进行高性能同步，无需担心重复执行导致数据错乱。

---

## 二、操作流程总览

整体迁移流程分为三个阶段：

```
第 1 步：配置旧版数据源 → 第 2 步：测试连接 → 第 3 步：执行迁移任务
填写源数据库连接信息       验证连通性          选择模块与参数，启动迁移
```

1. **配置旧版数据源**：进入迁移配置，填写源数据库连接信息。
2. **测试连接**：验证连通性，确保测试连接成功。
3. **执行迁移任务**：进入迁移任务明细，选择迁移模块与参数，启动一键全量 / 增量迁移。

---

## 三、详细操作步骤

### 步骤 1：新建旧版数据源配置

进入「迁移配置」页面，点击新建数据源，按要求填写以下连接信息（带 * 为必填项）：

| 字段 | 说明 | 填写示例 |
| --- | --- | --- |
| 数据源名称 * | 可随意输入，自定义名称，便于识别区分 | contentlive 旧系统库 |
| 主机地址 * | 填写数据库的**外网地址**，域名或 IP 均可（不能填 `127.0.0.1`） | db-xxx.example.com 或 1.2.3.4 |
| 连通端口 * | 数据库连接端口，默认 `3306`（无特殊配置时）。注意：**安全组需放通该端口** | 3306 |
| 数据库名 * | 填写 app 数据库的名称 | app |
| 用户名 * | 可用 `root`（建议使用 app 数据库对应的用户名） | root |
| 源库密码 | 根据所填用户名填写对应密码。如用户名为 `root`，则填 root 密码 | 请输入对应密码 |

> 💡 数据源名称建议清晰可辨（如标注环境、用途），后续迁移任务将通过该名称选择对应数据源。

填写完成后点击「确定」保存配置。

### 步骤 2：测试连接

数据源保存后，点击「测试连接」按钮，系统将验证所填连接信息的连通性。

- **测试成功**：表示数据源配置正确，可继续下一步。
- **测试失败**：请检查主机地址、端口、用户名及密码是否填写正确，确认源库网络是否可访问后重新测试。

> ⚠️ 务必在测试连接成功之后再进行后续迁移操作，避免因连接异常导致迁移任务失败。

### 步骤 3：执行一键数据迁移（全量 + 增量）

测试连接成功后，点击「迁移任务明细」，再点击「一键全量 / 增量迁移」，进入迁移任务配置界面。

#### 3.1 选择数据源配置（必填）

在「数据源配置」下拉框中，选择步骤 1 中配置好的旧版数据源。

#### 3.2 选择迁移模块（必填）

勾选需要迁移的业务模块。引擎会自动解析依赖顺序，并补充上游必须数据，无需手动处理模块间的依赖关系。可迁移模块包括：

| 模块分组 | 包含项 |
| --- | --- |
| 内容结构 | 分类 / 频道、标签、播放器及 Lua 脚本 |
| 视频资源 | 视频、视频线路 |
| 用户数据 | 用户、播放记录、收藏、评论、弹幕 |
| 运营数据 | 金币 / 流水、会员 VIP、卡密 |

> 💡 如需迁移全部数据，可直接点击右上角「⚡ 一键选择全量链路」，系统将自动勾选所有关联模块。

#### 3.3 实时增量同步（暂不可用）

「实时增量同步」开关当前处于功能升级中状态（待 binlog 改造），暂不可用。当前阶段请使用全量 + 增量的重复迁移机制完成数据同步。

#### 3.4 启动迁移

确认数据源与迁移模块选择无误后，点击「确定并执行」，系统即开始执行迁移任务。

---

## 四、注意事项

- **可重复执行**：得益于 Upsert 与防重复跳过机制，迁移任务可随时重复发起，引擎只会同步新增或变动的数据，不会产生重复数据。
- **依赖自动处理**：勾选模块时无需关心依赖关系，引擎会自动解析并补全上游必需数据。
- **先测试后迁移**：执行迁移前，请务必确保数据源「测试连接」通过。
- **增量功能状态**：实时增量同步功能正在升级中，当前请通过重复发起迁移任务的方式实现增量同步。
