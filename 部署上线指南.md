# 部署上线指南：让别人在线访问 + 改完自动同步

本指南教你把这个文档网站发布到互联网，得到一个网址发给别人；以后你改了内容，网站会自动更新。

全程在网页上点击操作，**不需要用命令行**。

---

## 一、原理（先看一眼，30 秒）

```
你改 .md 文件  →  提交到 GitHub 仓库  →  GitHub 自动重新发布  →  网址内容更新
```

- **GitHub** 既是存放文件的地方，也是"改完自动同步"的引擎。
- **GitHub Pages** 是 GitHub 自带的免费网站托管，把仓库变成一个公开网址。
- 这套组合：免费、自动配 HTTPS、可绑自定义域名。

---

## 二、首次部署（约 10 分钟）

### 第 1 步：注册 GitHub

打开 https://github.com，注册一个账号（免费）。

### 第 2 步：新建仓库（Repository）

1. 登录后，点右上角 `+` → `New repository`。
2. **Repository name** 填一个名字，例如 `contentlabs-docs`。
3. 选择 **Public**（公开）。
   > 免费版必须是 Public 才能用 GitHub Pages。如果内容不能公开，见文末「不想公开怎么办」。
4. 其他不用动，点 `Create repository`。

### 第 3 步：上传网站文件

1. 在新仓库页面，点 `Add file` → `Upload files`。
2. 把本文件夹里的**所有内容**拖进上传框，**必须包含**：
   - `index.html`
   - `README.md`、`_sidebar.md`、`_navbar.md`
   - 所有 `01~06-*.md`
   - `vendor/` 整个文件夹（这是网站的运行依赖，缺了会白屏）
   - `.nojekyll`（重要！下划线开头的文件靠它才不会被忽略）

   > 提示：可以直接把文件夹里的东西全选拖进去。如果浏览器不支持拖文件夹，就用下面「方式 B」。
3. 下方填一句说明（如 `init`），点 `Commit changes`。

> **如果 `.nojekyll` 看不到**：它是隐藏文件。Mac 按 `Cmd+Shift+.` 显示隐藏文件；Windows 在文件夹「查看」里勾选「隐藏的项目」。或者上传后在 GitHub 网页点 `Add file → Create new file`，文件名输入 `.nojekyll`，内容留空，提交即可。

### 第 4 步：开启 GitHub Pages

1. 进仓库 → 顶部 `Settings`。
2. 左侧菜单点 `Pages`。
3. **Source** 选 `Deploy from a branch`。
4. **Branch** 选 `main`，文件夹选 `/ (root)`，点 `Save`。
5. 等 1~2 分钟，刷新这个页面，顶部会显示：
   ```
   Your site is live at https://你的用户名.github.io/contentlabs-docs/
   ```

把这个网址发给任何人，都能在线访问。✅

---

## 三、以后怎么改内容（自动同步）

### 方式 A：直接在 GitHub 网页上改（最简单）

1. 进仓库，点开要改的文件（如 `03-user.md`）。
2. 点右上角铅笔图标 ✏️ `Edit this file`。
3. 改完，拉到底点 `Commit changes`。
4. 等 1~2 分钟，网站自动更新。

### 方式 B：在电脑上改好再上传

1. 在自己电脑上改好 `.md` 文件。
2. GitHub 仓库 → `Add file` → `Upload files` → 拖入修改后的文件（同名会覆盖）。
3. `Commit changes`，等 1~2 分钟自动更新。

> 无论哪种方式，你都**不用碰服务器、不用重新构建**。Docsify 是实时渲染的，文件一变，网站就变。

---

## 四、可选增强

### 绑定自己的域名

如果你有自己的域名（如 `docs.gguo.com`）：

1. 在域名服务商后台加一条 DNS 记录，指向 GitHub Pages（CNAME 指到 `你的用户名.github.io`）。
2. 仓库 `Settings → Pages → Custom domain` 填入你的域名，Save。
3. 勾选 `Enforce HTTPS`。

### 不想公开内容怎么办

GitHub 免费版的 Pages 要求仓库公开。若内容必须私有，有几个选择：

- 升级 GitHub Pro（私有仓库也能开 Pages）。
- 换用 **Cloudflare Pages** 或 **Netlify**：同样连 Git 仓库、改完自动同步，免费额度大，且支持私有仓库 + 访问密码保护。步骤与本指南类似（连仓库 → 自动发布）。
- 部署到自己的服务器（见 `使用说明.md` 里的 Nginx 配置），配合内网或 Basic Auth 访问控制。

---

## 五、常见问题

**网址打开是空白 / 一直转圈？**
多半是 `vendor/` 文件夹或 `.nojekyll` 没传上去。检查仓库里这两样在不在。

**改了内容网站没变？**
等 1~2 分钟；再不行清一下浏览器缓存（Ctrl/Cmd + Shift + R 强制刷新）。

**侧边栏 / 导航不显示？**
确认 `_sidebar.md` 和 `_navbar.md` 都已上传（下划线开头，靠 `.nojekyll` 才不被忽略）。

**想换平台（Gitee / Cloudflare / Netlify）？**
原理完全一样：把这个文件夹连到对应平台的「从 Git 仓库部署」即可。国内访问 GitHub Pages 偶尔慢，可考虑 Gitee Pages 或 Cloudflare Pages。
