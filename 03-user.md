# 用户管理操作手册

> 适用页面：运营管理 → 用户管理
> 适用对象：租户运营
> 阅读方式：本手册按"运营要做的三件事"组织——**让用户进来、管理用户、处置违规用户**。遇到具体场景时，直接翻到对应主线即可。

---

## 开始之前：理解一个核心规则

用户在列表里显示的状态（正常 / 风控中 / 禁言中 / 已封禁），**不是运营直接设置的**，而是系统根据处置动作自动得出的结果。

```
你做了什么动作        →   用户呈现什么状态
─────────────────────────────────────────
什么都没做            →   🟢 正常
（系统自动风控）      →   🟡 风控中
你禁言了他            →   🟡 禁言中
你封禁了他            →   🔴 已封禁
```

所以运营不会去"改状态"，而是去"做动作"（封禁、禁言等），状态会自动跟着变。当用户同时具有多个状态时，列表显示最重的那个（已封禁 > 风控中 / 禁言中 > 正常）。

理解这一点后，后续所有操作都更容易理解。

---

## 主线一：让用户能进来

用户进入平台有两条路径，根据场景选择。

### 场景 A：运营手动为某人开通账号

适合内部测试、客服代开号、导入少量种子用户。

操作：用户管理页 → 左上角「+ 新增用户」→ 填写用户名、邮箱、初始密码 → 保存。

完成后账号立即可用，**无需任何邮件配置**。

### 场景 B：开放用户自助注册

这是面向真实用户的常规方式，但有一个**硬性前置条件**：

> ⚠️ 用户注册时，系统需向其发送验证码邮件。**如果邮件发不出去，用户就收不到验证码，无法注册。** 因此开放注册前，必须先配置好邮件并测试通过。

整条主线如下：

```
第 1 步：配置邮件 → 第 2 步：点测试验证 → 第 3 步：测试通过才开放注册
                                              ↓
                            用户注册 → 系统发验证码 → 用户验证 → 进入平台
```

#### 第 1 步：配置邮件

邮件配置页需填写两块内容。

**服务器链接**（告诉系统用哪个邮箱发信）

| 字段 | 填写说明 |
| --- | --- |
| SMTP 服务器 | 邮件服务商的服务器地址，如 `mail.contentlive.com` |
| 端口 | 邮箱服务商提供的端口号，常见为 465 |
| 加密传输（SSL/TLS） | 建议开启 |
| 认证用户名 | 通常为完整邮箱地址，如 `admin@contentlive.com` |
| 密码 / 授权码 | 邮箱密码或授权码（**QQ、163 邮箱必须用授权码，而非登录密码**） |

**发件人信息**（用户收到邮件时看到的内容）

| 字段 | 填写说明 |
| --- | --- |
| 发件人邮箱 | 邮件的发件地址，部分邮箱要求与认证用户名一致 |
| 显示名称 | 用户看到的发件人名字，建议填 App 名称，降低被判为垃圾邮件的概率 |

#### 第 2 步：点「测试」验证

填完后**务必点「测试」按钮**，系统会试发一封邮件：

- ✅ 成功 → 配置无误，进入第 3 步。
- ❌ 失败 → 配置有误，对照"常见问题"修正后重测。

> ⚠️ 切勿跳过测试直接开放注册——配置错误会导致所有用户都收不到验证码。

#### 第 3 步：开放注册

测试通过后，用户即可在 App 端用邮箱自助注册。

### 邮件相关的其他配置

除发信配置外，邮件模块还有几项配置，按需使用。

**邮箱后缀白名单**

控制**哪些后缀的邮箱才允许注册**。配置后只有白名单内的后缀（如 `@gmail.com`、`@qq.com`）才能注册，其他后缀会被拒绝。

用途：过滤临时邮箱、一次性邮箱等高风险来源，降低批量注册的垃圾账号。

**邮件模板**

支持**自定义邮件模板**，包括文字内容和 UI 显示效果（富文本配置）。可为验证码、找回密码等不同场景配置各自的模板，调整文案、排版、品牌样式，让用户收到的邮件更专业、更贴合 App 形象。支持添加多个模板按场景使用。

**发送记录**

查看邮件的**发送历史**——发给了谁、内容是什么、何时发送、是否成功。

用途：排查"用户说没收到邮件"时，先看发送记录确认系统是否发出，快速定位是发送失败还是用户那边的问题。

---

## 主线二：管理一个具体用户

日常运营中，经常需要找到某个用户、查看其情况、调整其资产。本主线说明具体做法。

### 第 1 步：找到这个用户

用户管理页顶部有筛选区，支持组合查询。

**按身份查找**：用户名 / 邮箱 / 手机号。

**按特征查找**（风控排查常用）：

- 用户状态：正常 / 风控中 / 禁言中 / 已封禁（可多选）
- 违规次数：如 `> 10 次`
- 金币数量：如 `> 10000`
- 会员状态 + 到期时间：选 VIP / SVIP 后，可筛快到期或长期会员

填好条件点「搜索」，点「重置」清空。

> 💡 示例：想揪出可疑的刷量用户，可筛"违规次数 > 10 且 金币 > 5000"，快速定位一批高风险账号。

### 第 2 步：打开用户详情

在列表中找到用户，点「管理」，打开详情抽屉。抽屉顶部有四个关键数据：当前金币、会员状态、在线设备、违规次数。抽屉内有四个 Tab，按需查看。

**查看 / 修改账号信息（基本信息 Tab）**

查看用户名、邮箱、手机号、注册时间、最近登录等。

- 改信息：点「编辑」，页面变为编辑态（背景变黄提示"编辑中"），改完点保存。
- 重置密码：编辑态下直接修改密码字段，可点「随机」一键生成强密码，保存即生效。

**调整资产（资产权益 Tab）**

- 改会员：编辑会员等级、到期时间、下载配额。
- 调金币：点「调整余额」→ 选「增加」或「扣减」→ 填数量和原因 → 保存。每次调整自动留记录，可点「查看完整记录」追溯。

> 💡 快捷入口：抽屉顶部的"当前金币""会员状态"卡片右上角都有「编辑」按钮，点一下直接跳到对应调整界面，无需先切 Tab。

**查看用户行为（行为数据 Tab）**

顶部显示近 30 天关键指标（播放、收藏、互动、活跃天数）。下方可切换查看：

- 播放历史 / 收藏列表：用户看了什么、收藏了什么。
- 在线设备：当前登录的设备，可对可疑设备点「强制下线」。
- 互动历史：弹幕、评论记录，顶部可直接「设为禁言」。
- 任务进度：每日任务完成情况。

每个子项默认只显示最近几条。点「查看更多 / 查看完整记录」，可跳转到该项详细页面，查看该用户在该维度下的全部数据。

---

## 主线三：处置违规用户

这是运营对抗破解版、异常用户、黑产的核心工作。处置分三种手段，先理解区别，再根据实际情况自行判断使用。

### 三种处置手段

| 手段 | 谁触发 | 用户会怎样 | 怎么解除 |
| --- | --- | --- | --- |
| 系统风控 | 系统自动 | 看激励视频才能继续使用 | 满足条件后自动放开 |
| 禁言 | 运营手动 | 不能发弹幕评论，但能正常观看 | 运营手动解除，或到期自动解 |
| 封禁 | 运营手动 | 完全无法登录 | 运营手动解禁，或到期自动解 |

**关于系统风控**

系统风控由系统自动判断和执行。系统会根据用户**是否依据正常的加解密算法**以及**风控特征**等维度自动识别异常，命中条件时对用户进行风控（用户需观看激励视频才能继续使用）；当用户行为重新满足正常条件时，系统会**自动放开**风控。

运营可通过「全局风控策略」配置这套规则——如触发风控的条件、风控的维度、风控时长等。配置后系统按规则自动执行。

> 📌 系统风控是自动机制，针对的是"可恢复的异常"。如某个用户需要更强的处置，运营可使用禁言或封禁。

### 三种手段如何选择

禁言、封禁的使用时机由运营根据实际情况判断：

- **禁言**：限制用户发言，但不影响其观看。
- **封禁**：让用户无法登录使用，是更重的处置手段。

具体何时使用哪种，由运营结合违规性质、严重程度自行决定。

### 操作：禁言

- **单个**：列表行点「禁言」，或在用户详情"行为数据 → 互动历史"顶部点「设为禁言」。选时长即可。
- **批量**：列表勾选多个用户 → 顶部「批量处理」→「批量禁言」。

### 操作：封禁

封禁是让用户登不上。封禁支持选择"维度"——封账号还是封设备，作用范围不同。

| 封禁维度 | 作用范围 |
| --- | --- |
| 仅账号 | 该账号无法登录，但同一设备上可注册 / 登录其他账号 |
| 仅设备 | 该设备不允许注册新账号，也不允许登录任何账号 |
| 账号 + 设备 | 账号和设备同时生效 |

> ⚠️ 设备维度的封禁作用范围较广（会影响该设备上的所有账号），属于较重的处置手段，请根据实际情况谨慎使用。

**快速封禁（账号维度）**

列表行点「封禁」→ 选时长（默认 3 小时）→ 填原因 → 确认。此方式固定封账号，最快。

**完整封禁（含设备、清资产、连带禁言）**

进入用户详情「风控 Tab → 运营封禁 → 封禁」，此处能力最全：

- 维度：账号 / 设备 / 账号 + 设备
- 时长：1 小时到永久
- 清零资产：勾选后可同时清空金币、会员、下载配额
- 同时禁言：封禁的同时禁言

**批量封禁**

列表勾选多个用户 → 顶部「批量处理」→「批量封禁」→ 选时长和原因。

> ⚠️ 批量封禁固定账号维度，且**不提供清零资产选项**——防止运营一次性误清大量用户的资产。需要清零资产请在单个用户详情里操作。

### 操作：解禁 / 解除禁言

- **解禁**：列表行点「解禁」。若用户只封了一个维度，直接解除；若账号和设备都封了，会让你选择解除哪个。
- **解除禁言**：列表行点「解除禁言」，或在互动历史顶部点解除。

### 关联登录用户

在用户详情「风控 Tab → 关联登录用户」，可查看**与当前用户在同一台设备登录过的其他账号**。可勾选这些关联账号，进行批量封禁 / 批量解禁。

### 全局风控策略

列表顶部「全局风控配置」，配置系统自动风控的规则——如触发风控的条件、风控的维度、风控时长等。配置后系统按规则自动执行，这是前面所说"系统风控"的来源。

---

## 附一：常见问题

**用户注册收不到验证码？**
先看邮件「发送记录」确认系统是否发出；再在配置页点「测试」确认配置正常；让用户查垃圾邮件文件夹；确认发件人邮箱与认证用户名一致；确认 QQ / 163 用的是授权码而非登录密码。

**用户注册时提示邮箱不被允许？**
检查「邮箱后缀白名单」，确认该用户的邮箱后缀是否在允许列表内。

**QQ / 163 邮箱测试一直失败？**
这两家必须用授权码，而非登录密码。登录邮箱网页版 → 设置 → 开启 SMTP 服务 → 生成授权码，把授权码填进"密码 / 授权码"字段。

**系统风控和封禁有什么区别？**
系统风控由系统自动判断和执行，满足正常条件后自动放开；封禁是运营手动操作，用户无法登录，需运营手动解禁或等待到期。

**封禁后用户换个账号又回来了？**
若只封了账号维度，用户换账号仍可在同设备使用。如需让该设备无法继续承载任何账号，可选择设备维度封禁（请谨慎使用）。

**用户状态能不能直接改成"正常"？**
不能。状态是系统根据处置动作自动派生的。要让用户恢复正常，去解禁 / 解除禁言，状态会自动变回去。

**调金币、改会员会留记录吗？**
会。金币调整自动写入变动记录，在"资产权益 → 金币余额 → 查看完整记录"可查到。

**怎么自定义注册邮件的样式？**
在「邮件模板」里配置，支持富文本（内容 + UI 显示效果），可按场景添加不同模板。

---

## 附二：开放注册前 Checklist

- [ ] SMTP 服务器、端口、认证用户名、密码 / 授权码都已填（QQ / 163 用授权码）
- [ ] 加密传输已开启
- [ ] 发件人邮箱与认证用户名一致（如服务商要求）
- [ ] 显示名称已填（建议用 App 名称）
- [ ] **已点「测试」且显示成功**
- [ ] 实际收到测试邮件、内容正常
- [ ] （如需限制）邮箱后缀白名单已配置
- [ ] （如需自定义）邮件模板已配置
