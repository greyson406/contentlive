# 采集到播放完整流程操作手册

> 适用页面：内容管理 → 视频分类 / 线路管理 / Lua 脚本组 / 采集 / 视频管理；推荐管理
> 适用对象：运营 / 内容管理人员
> 阅读方式：本手册介绍从「配置一个全新的 App」到「用户能在端上看到视频播放」的完整链路，按操作时序串联，照着做即可完成全套配置。

---

## 一、整体架构

在开始操作之前，先理解几个核心模块之间的关系：

| 模块 | 作用 | 配置位置 |
|------|------|---------|
| **视频分类（频道）** | 定义平台有哪些频道、子分类（电影/电视剧/动漫…） | 内容管理 → 视频分类 |
| **播放线路** | 定义视频用什么协议、走哪个 CDN 播放、是否需要解析 | 内容管理 → 线路管理 |
| **Lua 脚本组** | 当线路需要复杂解析（加密、签名）时使用 | 内容管理 → Lua 脚本组 |
| **采集源** | 从第三方资源站抓取视频元数据和播放地址 | 内容管理 → 采集 |
| **视频库** | 采集入库后的最终视频数据，对接 App 端展示 | 内容管理 → 视频管理 |

**模块依赖关系**：

```
        Lua 脚本组（可选）
              │
              ▼
视频分类 → 播放线路 → 采集源 → 视频库 → App 端播放
              ▲
              │
        （线路绑定脚本组）
```

**数据流向**：

```
[采集源 抓取] → [按判重规则识别同/异视频] → [按分类映射归入本地频道]
              → [按更新策略写入视频库] → [App 端选择播放线路播放]
```

---

## 二、完整链路示意（速览）

```
┌─────────────────────────────────────────────────────────────────┐
│                       一次性配置（系统初始化）                    │
├─────────────────────────────────────────────────────────────────┤
│  ① 视频分类  ──→  ② 播放线路  ──→  ⑤ 全局策略                   │
│     (频道)         │                 (判重/更新/合并/安全)        │
│                    └─→ ② Lua 脚本组（可选，需复杂解析时）         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       每接入一个采集源                            │
├─────────────────────────────────────────────────────────────────┤
│  ③ 添加采集源（测试连通 ✅）                                      │
│         │                                                        │
│         └─→ ④ 绑定远程分类 → 本地频道                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                          日常运营                                 │
├─────────────────────────────────────────────────────────────────┤
│  ⑥ 触发采集（手动 / 自动）                                       │
│     │                                                            │
│     ├─→ 判重规则识别 → 跳过已存在 / 更新指定字段 / 新增          │
│     ├─→ 分类映射 → 归入本地频道                                  │
│     ├─→ 安全过滤 → 命中拒收                                      │
│     └─→ 入库 → 视频库                                            │
│                                                                  │
│  ⑦ 验证 → 采集日志 + 视频管理 + App 端播放                       │
│                                                                  │
│  App 端播放时:                                                   │
│     视频 → 选择线路 → 直链 OR Lua 解析 → 拿到播放地址 → 播放     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       视频运营加工（视频管理）                    │
├─────────────────────────────────────────────────────────────────┤
│  ⑧ 编辑视频:                                                     │
│     ├─ 基本信息  → 标签 / 别名 / 简介 / 互动数据 / 追剧日历     │
│     ├─ 播放源    → 集数列表 / 跨线路指定                        │
│     └─ 投放与高级 → Banner 投放 / 会员计费                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       运营策略编排（运营策略）                    │
├─────────────────────────────────────────────────────────────────┤
│  ⑨ 聚合编排（与单剧快捷配置双向同步）:                          │
│     ├─ Banner 推荐管理 → 标题/位置/链接类型/展示时间/状态 / 预览 │
│     └─ 更新计划管理    → 更新日/时间/排序/状态/有效期（仅预告）  │
│                              │                                   │
│                              ▼                                   │
│                       呈现在 App 侧                              │
└─────────────────────────────────────────────────────────────────┘
```

---
---

## 三、前置准备（必做）

> ⚠️ **重要**：以下两步必须在采集前完成。如果没有频道、没有线路，采集到的数据无法入库或无法播放。

### 步骤 1 · 配置频道（视频分类）

**入口**：内容管理 → 视频分类

#### 1.1 新增顶级分类（频道）

点击页面左上角 `+ 新增顶级分类` 按钮，进入新增弹窗：

| 字段 | 说明 | 示例 |
|------|------|------|
| **父级分类** | 顶级分类填"顶级分类（无父级）"，子分类选择父级 | 顶级分类 |
| **分类名称** | 展示给用户看的频道名称（最长 50 字） | 电影、电视剧、动漫、综艺、短剧 |
| **标识码** | 唯一标识，**创建后不可修改**。建议点击 `自动生成` 按钮根据名称生成 | movie、tv、anime、variety、short |
| **图标** | 频道图标 URL，App 端 Tab 栏可能用到（可选） | https://cdn.xxx.com/icon-movie.png |
| **排序** | 数字越小越靠前，决定 App 端频道展示顺序 | 0、10、20… |
| **描述** | 内部备注用，不展示给用户（最长 200 字） | 院线大片、海外电影合集 |
| **初始状态** | 启用 / 禁用（禁用的分类不展示给用户） | 启用 |
| **竖屏模式** | 当前已支持横屏 + 竖屏，竖屏已实现类似红果的"刷剧"形态体验。**后续会进一步区分长视频竖屏 vs 短剧竖屏的差异化交互**，提供更优体验 | 横屏（默认） |

> 💡 **关于竖屏模式**：
> - **当前已实现**：横屏和竖屏都支持，竖屏已经做了类似红果那样的全屏滑动刷剧体验
> - **后续优化方向**：在已有基础上做更精细的分层——
>   - **长视频的竖屏模式**：竖屏播放但保留长视频的浏览/查找路径
>   - **短剧的竖屏模式**：纯刷剧形态，强滑动信息流 + 专属推荐策略
>   - 针对两种场景做差异化的用户交互细节

#### 1.2 新增子分类

顶级分类创建好后，鼠标 hover 到该行，点击 `+ 新增子分类`：

- **父级分类**：自动填入当前顶级分类（如"电影"）
- 其他字段填法同上
- 例如：电影下面新增「动作片 / action」「喜剧片 / comedy」「纪录片 / documentary」

#### 1.3 推荐的分类骨架

第一次搭建时建议参考以下结构（可根据业务调整）：

```
电影 (movie)
├── 动作片 (action)
├── 喜剧片 (comedy)
├── 爱情片 (romance)
├── 科幻片 (scifi)
├── 恐怖片 (horror)
└── 纪录片 (documentary)

电视剧 (tv)
├── 国产剧 (cn_drama)
├── 港澳剧 (hk_drama)
├── 日剧 (jp_drama)
├── 韩剧 (kr_drama)
└── 欧美剧 (us_drama)

动漫 (anime)
├── 国漫 (cn_anime)
├── 日漫 (jp_anime)
└── 欧美动漫 (us_anime)

综艺 (variety)
├── 大陆综艺 (cn_variety)
└── 韩国综艺 (kr_variety)

短剧 (short)
```

#### 1.4 验证

返回视频分类列表，确认底部统计 `X 顶级 / X 总计 / X 启用` 数值符合预期。

---

### 步骤 2 · 配置播放线路

**入口**：内容管理 → 线路管理

> 💡 **系统已预置 rrys 线路**，开箱即用。**新人最简路径**：直接配置预置线路的密钥即可开始用，不需要自己新建。

#### 2.1 最简路径：使用系统预置线路（推荐）

系统默认植入预置线路：

| 线路名称 | 编码 | 类型 | 用途 |
|---------|------|------|------|
| rrys | rrys | 系统 | 配套 rrys 采集源使用 |

**配置步骤**：

1. 在线路列表中找到预置线路，点击操作列的 `更多 → 密钥`
2. 进入密钥配置页面，填写：
   - **App ID**：从服务商后台获取
   - **App Key**：从服务商后台获取
3. 保存 → 这条线路就可以正常使用了

> ⚠️ 密钥未配置的线路会导致播放失败。新接入系统时务必先把密钥配好。

#### 2.2 进阶路径：新建自定义线路

如果需要接入第三方解析服务、自建 CDN，或想为不同会员等级提供差异化线路，可以新建：

点击页面左上角 `+ 新建线路`，弹窗有两个 Tab：

##### 2.2.1 基本设置 Tab

| 字段 | 说明 |
|------|------|
| 状态 | 启用/禁用 |
| 排序 | 数字越小越靠前，决定 App 端线路 Tab 顺序 |
| **线路名称** | 展示给用户的名称，如"高清线路 1"、"会员专享线路" |
| **英文标识（编码）** | **必须与采集源中的「来源」字段值保持一致**，系统据此把采集到的视频归到对应线路。创建后不可修改 |
| 全局计费模式 | 默认跟随（不配置）。如需特殊计费可指定 |
| **VIP 等级门控** | **关键字段**：默认"公开（所有用户可见）"。**设置后只有达到该 VIP 等级的用户才能使用此线路** —— 用此字段为会员提供更高质量的专属线路（具体文案后续完善） |
| 竖屏模式 | 同视频分类的竖屏模式说明，当前默认横屏 |
| 备注 | 后台备注信息 |
| 提示 | 前台显示给用户的提示文字 |
| 描述 | 线路用途说明 |

##### 2.2.2 解析配置 Tab

> 💡 **顶部提示**：绑定脚本组后，客户端播放时将通过统一解析入口获取视频地址。**服务端模式**直接返回清晰度列表；**客户端模式**下发 Lua 脚本供客户端解析。

按是否需要 Lua 解析分两种配法：

**A. 直链播放（最简单）**

视频地址本身就是可播放的 M3U8/MP4，不需要二次解析。

| 字段 | 填法 |
|------|------|
| 启用 Lua 解析 | **不启用（直接播放）** |
| 绑定脚本组 | 不选 |
| 直连解析 URL | 选填，例如 `https://example.com/jx?url=`。**客户端实际播放地址 = [直连解析 URL] + [视频源的播放地址/ID]** |
| 指定 User-Agent | 选填，部分资源对 UA 有校验时填写 |
| 指定 HTTP Header | 选填，必须为合法 JSON 格式，例如 `{"Referer":"https://example.com"}` |

**B. 需要 Lua 解析（复杂资源）**

加密资源或需要签名的资源，必须通过 Lua 脚本完成解析。

| 字段 | 填法 |
|------|------|
| 启用 Lua 解析 | **启用 Lua 脚本组** |
| 绑定脚本组 | 选择已创建的脚本组（如果还没建，先去 [步骤 2.5](#步骤-25--配置-lua-脚本组可选)） |
| 下发模式 | 跟随脚本组配置（推荐）。如需覆盖可单独指定 |

> ⚠️ **前置条件**：选择"启用 Lua 脚本组"前，必须先在 **Lua 脚本组** 页配置好对应脚本组，详见步骤 2.5。

##### 2.2.3 测试线路连通性

线路保存后，在列表操作列点击 `更多 → 测试`：

- **方式 1**：直接选择该线路下已有的视频测试
- **方式 2**：输入任意视频的播放链接测试

测试通过后，这条线路才算真正可用。

---

### 步骤 2.5 · 配置 Lua 脚本组（可选）

> 💡 **何时需要**：只有线路的解析配置选了"启用 Lua 脚本组"才需要。直链播放的线路可以跳过此步骤。

**入口**：内容管理 → Lua 脚本组

#### 2.5.1 新建脚本组

点击 `+ 新建脚本组` → 进入编辑弹窗：

| 字段 | 说明 |
|------|------|
| 脚本组名称 | 如"rrys 通用解析"、"xxx 资源站解析" |
| 下发模式 | **服务端执行（推荐）** / 客户端执行 |
| 默认清晰度 | 720p 高清 / 1080p / 原画 等 |
| 描述 | 脚本组用途说明 |

> 💡 **关键机制**：创建脚本组时，系统会**自动生成一个解析脚本和一个签名脚本**（code 由系统自动分配）。你只需要点击「编辑脚本」直接跳转到编辑器修改内容即可。

#### 2.5.2 编辑脚本

回到列表，找到刚创建的脚本组，点击 `编辑脚本`：

- **解析脚本**：负责把原始播放地址解析成可播放的 URL
- **签名脚本**：负责生成播放请求需要的签名（如果资源站需要）

在代码编辑器中粘贴 Lua 代码 → 保存。

#### 2.5.3 变量管理

如果脚本中用到了配置变量（如 App Key、加密盐），在列表操作列点击 `变量管理`：

- 新增变量：变量名、变量值
- 这些变量在脚本中可以通过预定义函数读取，避免硬编码密钥

#### 2.5.4 脚本组的复用

**脚本组可以跨线路复用**：

- 一个脚本组配置好后，可以被多条线路绑定
- 如果多条线路用的是同一家服务商的解析逻辑，**只需配置一次脚本组**，后续线路直接绑定即可
- 修改脚本组时，所有绑定它的线路会同步生效

#### 2.5.5 回到线路绑定

脚本组配好后，回到 **线路管理 → 编辑线路 → 解析配置 Tab**：

1. 启用 Lua 解析 = "启用 Lua 脚本组"
2. 绑定脚本组 = 选择刚才创建的脚本组
3. 保存 → 完成线路配置

#### 2.5.6 内置脚本组

系统默认配套 rrys 内置脚本组，对应预置 rrys 线路，无需自己写脚本，配好密钥/变量即可使用。

---

## 四、采集核心流程

### 步骤 3 · 添加采集源

**入口**：内容管理 → 采集 → `+ 添加采集源`

> 💡 **系统已预置 rrys 采集源**，新接入系统时可以直接拿来用，验证流程通了再添加自己的源。如果只用内置源，本步骤可跳过。

#### 3.1 准备工作

添加采集源之前，**确认前置依赖都已就绪**：

- ✅ 视频分类已配置（步骤 1）
- ✅ 播放线路已配置且测试通过（步骤 2）
- ✅ Lua 脚本组已配置（如果线路需要，步骤 2.5）

如果上述任意一项没准备好，采集到的数据要么入不了库，要么入库后播不出来。

#### 3.2 获取采集源信息

在添加之前，需要从资源站方拿到以下信息：

| 信息 | 说明 |
|------|------|
| 采集接口 URL | 资源站提供的苹果 CMS JSON 接口地址，形如 `https://xxx.com/api.php/provide/vod/at/json/` |
| 协议类型 | 一般是苹果 CMS JSON（V10 标准），少数老站点用 XML |
| 是否需要密钥 | 部分资源站对接口做了鉴权 |
| 资源站分类列表 | 用于后续分类映射，提前知道有哪些分类好做规划 |

#### 3.3 填写基础配置 Tab

点击 `+ 添加采集源` 进入新增弹窗：

| 字段 | 说明 | 填写示例 |
|------|------|---------|
| **采集源名称** | 内部识别用，建议用简短的中文或拼音 | 资源站A、热播资源、xxx 影视 |
| **协议类型** | 默认选 **苹果 CMS JSON（推荐）** | 苹果 CMS JSON |
| **采集接口地址** | 资源站提供的完整 URL | `https://example.com/api.php/provide/vod/at/json/` |
| **测试连通** | **填完地址必须点这个按钮** | 点击后会发起一次请求，弹窗显示连通性结果 |
| 状态 | 启用 | 启用 |
| 备注 | 可选，记录这个源的特点 | "更新最快的国漫源" |

> ⚠️ **必做：测试连通**
> - 点击「测试连通」按钮，系统会请求该接口并尝试解析返回数据
> - 返回 ✅ 成功：可以继续保存
> - 返回 ❌ 失败：检查 URL 是否正确、网络是否通、协议类型是否选对
> - **测试失败强行保存的采集源，自动采集时会全部失败**

#### 3.4 配置自动采集 Tab

| 字段 | 推荐配置 |
|------|---------|
| **自动采集开关** | 首次配置建议先关闭，手动测试一次成功后再开启 |
| **采集频率** | 预设：每 10 分钟 / 30 分钟 / 1 小时 / 2 小时 / 3 小时 / 6 小时 |
| **自定义 Cron** | 选填，需要特殊频率时填写 |

**频率选择参考**：

| 采集源角色 | 推荐频率 |
|----------|---------|
| 主力源（更新最快） | 每小时 |
| 备份源 | 每 3 小时 |
| 慢速大源 | 每 6 小时 |

> ⚠️ **不建议低于 10 分钟**：频率过高对资源站造成压力，容易被封 IP，且增量数据本来就有限。

#### 3.5 配置高级配置 Tab

| 区块 | 字段 | 推荐值 | 说明 |
|------|------|--------|------|
| **数据更新策略** | 跟随全局 / 单独配置 | **跟随全局** | 首次配置建议跟随全局，差异化需求再单独配 |
| **入库策略** | 默认入库状态 | 上架 | 采集到的视频默认对用户可见 |
| **内容清洗** | 数据净度算法 | 原样入库 | 后续发现标题杂乱再改"去水分" |
| **内容清洗** | 单独拦截年份 | 0 | 0 = 跟随全局阈值；填具体年份则在全局基础上叠加加严 |
| **内容清洗** | 单站屏蔽词 | 留空 | 后续发现该源经常采到不想要的内容再加 |
| **性能控制** | 最大采集页数 | 100 | 单次任务最多翻 100 页，防止失控 |
| **性能控制** | 请求间隔 | 500ms | 请求之间等待时间，防止被封 |

#### 3.6 保存采集源

点击 `保存`，回到采集源列表，新加的采集源出现在列表中。

#### 3.7 **关键下一步：绑定分类**

**采集源添加完成后，立刻去做分类绑定（步骤 4）**。否则即使触发采集，绝大多数视频都会因为"分类找不到本地归宿"而失败。

---

### 步骤 4 · 绑定远程分类 → 本地频道（关键步骤）

> ⚠️ **核心步骤**：未绑定的远程分类下的视频会因为找不到本地归宿而无法入库，这是新人最容易踩坑的地方。

#### 4.1 进入采集源浏览页

在采集源列表中点击采集源名称（或操作列「浏览」），进入该采集源的资源浏览页。

#### 4.2 查看顶部分类绑定栏

页面顶部显示该采集源的所有远程分类，每个分类的状态：

| 显示状态 | 含义 | 操作 |
|---------|------|------|
| `中国动漫 → 动漫 / 国漫` | 已绑定到本地频道 | 点击 chip 高亮筛选，✎ 图标改绑 |
| `短剧 → 未绑定`（红色虚线框）| 未绑定，该分类视频会被忽略 | 点击 chip 弹出绑定弹窗 |

#### 4.3 绑定操作

点击未绑定的远程分类 chip → 弹出绑定弹窗：

1. 弹窗顶部显示：远程分类「短剧」→ 选择本地频道/分类
2. 树形列表中选择本地分类（如「短剧 / 现代短剧」）
3. 点击 `确认绑定`

#### 4.4 解除绑定

绑定弹窗左下角的 `解除绑定` 按钮，恢复为未绑定状态。

#### 4.5 批量绑定建议

建议在**首次添加采集源后立即把所有远程分类都绑一遍**，避免后续采集任务执行时大量视频被跳过。

---

### 步骤 5 · 配置全局策略（一次配，全局生效）

**入口**：内容管理 → 采集 → `⚙ 全局策略`

> 💡 全局策略是平台的"默认基线"，单个采集源可以在编辑时选择"跟随全局"（推荐）或"单独配置"覆盖。

#### 5.1 判重规则

定义"什么算同一部视频"。当采集源没有提供影片 ID 时，用以下勾选的特征兜底判断。

| 维度 | 推荐 | 说明 |
|------|------|------|
| 影片名称 | ✓ 必选 | 严格匹配 |
| 上映年份 | □ 建议勾选 | 区分同名翻拍/续作 |
| 影片分类 | □ | 仅当数据混乱时启用 |
| 导演名称 | □ | 仅当数据混乱时启用 |

**勾选越多判定越严格**——例如同时勾选"名称+年份"，那么只有名称和年份都一致才算同一部视频。

#### 5.2 默认更新策略

定义"遇到已存在视频时怎么处理"。

| 模式 | 适用场景 |
|------|---------|
| 跳过不更新 | 一次性导入存量数据 |
| **更新指定字段（推荐）** | 日常运营 / 连续剧追更 |
| 全量覆盖 | 数据完全信任采集源（慎用，会丢失手动修改） |

**「更新指定字段」推荐勾选**：播放资源分组、更新集数进度、影片备注文字（其他字段一般不要勾，会覆盖运营手动修改的内容）。

#### 5.3 播放线路合并

定义"同名播放线路如何处理"。

| 选项 | 说明 |
|------|------|
| **内容直接覆盖（推荐）** | 用采集站最新资源组覆盖本地数据 |
| 追加为独立线路 | 作为备用线路追加到末尾 |
| 智能合并去重 | 按集数补全（本地缺失的从新数据补） |

#### 5.4 全局安全过滤

平台底线规则，命中即拒收。

| 字段 | 推荐 | 说明 |
|------|------|------|
| 年份自动拦截阈值 | 1900 | 小于此年份的数据全局拒收 |
| 全局来源拦截池 | 按业务填写敏感词 | 例如：澳门赌场、违规站名 |
| 全局屏蔽标题词 | 按业务填写 | 命中即拒收 |
| 全局请求超时 | 30 秒 | |
| 全局并发数 | 5 | |
| 失败重试次数 | 2 | |

---

### 步骤 6 · 触发采集

#### 6.1 手动采集（首次/补采用）

在采集源列表中，点击对应行操作列的「采集」按钮，弹出**采集范围弹窗**：

| 范围 | 适用场景 |
|------|---------|
| **采当天** | 日常补采，最常用 |
| 采近 3 天 | 漏采修复 |
| 采近 7 天 | 周期补采 |
| 采全部 | 首次接入新采集源（耗时长） |
| 自定义时间范围 | 精确补采某天的数据 |

点击 `开始采集`，任务提交后会跳转到「采集日志」可以看到任务进度。

#### 6.2 自动采集（日常运营）

只要在步骤 3.2 配置了自动采集开关 + 频率，系统就会按 Cron 自动触发。无需人工干预。

---

## 五、采集结果验证

### 5.1 在「采集日志」查看任务执行情况

**入口**：内容管理 → 采集 → 采集日志 Tab

每条任务记录展示：开始时间 / 采集源 / 触发方式 / 模式 / 总计 / 成功 / 跳过 / 失败 / 耗时 / 状态。

| 字段 | 含义 | 异常排查方向 |
|------|------|------------|
| 总计 | 此次任务处理的总条数 | — |
| 成功 | 成功入库或更新的数量 | — |
| 跳过 | 因判重已存在被跳过的数量 | 正常现象，无需处理 |
| **失败** | 入库失败的数量 | 点击「查看明细」查看错误原因 |

**常见失败原因**：
- 远程分类未绑定本地分类 → 回到步骤 4 补绑
- 命中全局/单站屏蔽词 → 检查屏蔽词配置
- 年份低于拦截阈值 → 检查全局安全过滤
- 接口请求超时 → 检查请求间隔和资源站稳定性

### 5.2 在「视频管理」查看入库视频

**入口**：内容管理 → 视频管理

按频道/分类筛选，确认新采集的视频出现在对应分类下。

### 5.3 在 App 端验证播放

打开 App 进入对应频道 → 找到新视频 → 点击播放，确认能正常起播。

---

## 六、视频管理（影视库管理）· 入库后的运营加工

**入口**：内容管理 → 视频管理

> 💡 采集流程负责"把视频拉进来"，视频管理负责"把视频加工好、投放好"。本节描述运营在视频入库**之后**最常用的查询、编辑、投放操作。

### 6.1 列表页能力速览

视频管理的列表是日常运营的主操作面板，包含三类能力：

| 区块 | 能力 |
|------|------|
| **筛选区** | 按名称/ID/别名、频道分类、付费类型、影片状态、影片地区、年份、完结状态、播放器 多维筛选 |
| **操作工具栏** | 新增视频、批量删除、一键清空、推荐配置、清除/重建缓存、导出、列设置 |
| **数据表格** | 影片名称、ID、封面、频道/分类、付费、地区、年份、集数、完结状态、播放器、上下架、互动数据、创建时间、标签、操作 |

### 6.2 列表行的基本操作

每行最右侧有两个操作 link：

- **编辑** — 打开编辑弹窗，对视频进行加工配置（详见 6.3）
- **删除** — 二次确认后从库中移除该视频

「状态」列的开关用于快速上下架，无需进入编辑弹窗。

### 6.3 编辑弹窗 · 三个 Tab

点击「编辑」打开右侧编辑弹窗，分三个 Tab：

| Tab | 用途 |
|------|------|
| **基本信息** | 核心标识、剧集状态、互动数据、更新计划（追剧日历） |
| **播放源管理** | 配置该剧的播放线路 + 集数列表 |
| **投放与高级** | Banner 投放、播放与计费（含付费会员限制） |

---

### 步骤 7 · 基本信息维护

#### 7.1 核心标识

| 字段 | 说明 |
|------|------|
| **名称** | 必填，剧的展示标题 |
| **副标题** | 可选，例如「第二季 · 复仇篇」 |
| **状态** | 上架 / 下架 |
| **搜索别名** | 支持添加多个别名，输入后按回车添加。**用于 App 端搜索时命中**：例如本剧名为《海贼王》，配置「航海王」后，用户搜索「航海王」也能找到 |
| **频道分类** | 必填，一级（电影/电视剧/动漫/综艺/纪录片/少儿）。采集入库时自动绑定（详见步骤 4 分类映射），运营可手动改 |
| **视频分类** | 非必填，二级，与频道分类联动（先选频道才能选视频分类） |
| **语言** | 手动输入，如「普通话 / 粤语 / 日语」 |
| **地区** | 选择剧的地区，如「内地 / 中国香港 / 日本 / 韩国」等 |
| **年份** | 选择上映年份 |
| **导演 / 演员** | 多人请使用中文全角逗号「，」分隔 |
| **标签** | 已添加的标签 + 建议标签池（推荐 / 热门 / 独家 / 新上线 / 即将下线 / 经典 / 高分 / VIP 专享）。**用于 App 端给该剧显示角标**。可点击建议快速添加，也可自定义输入文字按回车添加 |
| **封面图 URL** | 剧的封面图（竖版） |
| **剧集简介** | 直接输入纯文本，无需关心格式 |

#### 7.2 剧集状态 + 互动数据

**剧集状态：**

| 字段 | 说明 |
|------|------|
| 完结状态 | 连载中 / 已完结 |
| 总集数 | 与采集源对齐 |
| 已更新（连载数） | 当前已上的集数 |

**互动数据（子分组）：**

| 字段 | 是否展示给用户 | 说明 |
|------|--------------|------|
| 播放次数 | 不展示 | 仅作内部记录用，不会显示在 App 端 |
| **收藏次数** | ✅ 展示 | 会显示在 App 端剧详情/列表卡片上，可手动设置初始值用于冷启动 |
| **点赞次数** | ✅ 展示 | 会显示在 App 端剧详情/列表卡片上，可手动设置初始值用于冷启动 |

> 💡 收藏次数 / 点赞次数手动填入的值作为"基础值"，用户实际行为产生的增量仍会自动叠加。例如手填 1000，用户实际点赞 50 次后，App 端最终展示 1050。

#### 7.3 更新计划（追剧日历）

整个区块由「**展示在追剧日历**」开关控制，**默认关闭**。

- 关闭 → 仅作为内部数据，不对外
- 开启 → 该剧出现在 App 端「追剧日历 / 更新计划」页面

> ⚠️ 这里配置的更新日 / 时间只是**给用户看的更新预告**，不等于剧的实际更新时间。实际内容更新由采集任务决定。

**开启后的字段：**

| 字段 | 说明 |
|------|------|
| 更新日 | 周一 ~ 周日，可多选 |
| 更新时间 | HH:MM |
| 时间范围 | **跟随完结状态（默认）** / **手动设置** 二选一 |
| └ 跟随完结状态 | 自动取「剧首集上架时间 → 已完结时间」。连载中无结束时间 |
| └ 手动设置 | 更新开始时间、更新结束时间，**均可空，留空表示永久** |
| 排序优先级 | 多剧同时间更新时的排序，数字越小越靠前 |

> ⚠️ 此处修改自动同步至全局「更新计划」模块。

---

### 步骤 8 · 播放源管理

#### 8.1 集数行格式规则

每个播放源区块内是一段纯文本，每行一集，格式：

```
第0001集$021f2a04c26a4204931d661a1123d707
第0002集$360747b2e977407f9f5980c87f67602a
第0003集$f6018ce3ebbb49cfb7d9925092d4ea2a
```

格式说明：`第XXXX集$<资源标识>`，资源标识由采集源提供，通常是一串由字母数字组成的字符串。

#### 8.2 跨线路指定（特殊规则）

当某一集在当前线路下缺集或质量差，可指定从另一条线路取源：

```
第0003集$f6018ce3ebbb49cfb7d9925092d4ea2a$maos
```

| 项 | 规则 |
|------|------|
| 语法 | 在原行末尾再追加一个 `$<另一线路标识>` |
| 分隔符 | `$` 半角美元符号 |
| 段数 | 常规 2 段；跨线路 3 段（集名 + 资源标识 + 另一线路标识） |
| 跨线路标识 | 必须是该剧已配置的其他线路（如 `rrys` / `contentlive` / `maos`） |
| 影响范围 | 仅影响该集，不影响同线路下其他集 |
| 付费状态 | 跨线路取源后，该集的付费判断按"被跨向的那条线路"来 |

#### 8.3 播放源工具栏

每个播放源顶部提供工具按钮：清空、上移、下移、校正集数、格式互换、去前缀、删除。

底部「新增一组播放」按钮可为同一部剧追加多条线路。

---

### 步骤 9 · 投放与高级

#### 9.1 Banner 投放

**总开关默认关闭**，关闭时下方所有字段不展示。

**开启后必填字段：**

| 字段 | 必填 | 说明 |
|------|------|------|
| 海报图 URL（横版） | ✅ | Banner 配图，推荐 16:9 或 21:9 尺寸 |
| Banner 标题 | ✅ | 展示在 Banner 图上的主文案，长度建议 ≤30 字 |

**投放页面与位置表：**

每行配置一个投放位置：

| 列 | 说明 |
|------|------|
| 投放页面 | 首页 / 综艺频道 / 分类页 · 养生 / 会员页 / 搜索页推荐 等，从下拉列表选择 |
| 位置（位次） | 第 1 位 ~ 第 5 位 |
| 操作 | 👁 预览此页面 Banner / 🗑 移除 |

> ⚠️ **去重规则**：同一个投放页面在表格中**只能存在一次**。已添加过的页面在底部下拉中显示为「XX（已添加）」灰色禁用。

**单页预览**：点击每行的 👁 图标可弹出手机模拟图，看到该 Banner 在该页面、该位次的视觉效果。

**保存前请确认**：

- Banner 标题、海报图 URL 必填
- URL 必须以 `http://` 或 `https://` 开头
- 至少配置 1 行投放页面与位置

未满足以上条件时，系统会提示无法保存。

#### 9.2 播放与计费

| 字段 | 说明 |
|------|------|
| **计费模式** | 默认「跟随频道配置」（该剧的付费状态跟随所属频道）。如已配置 VIP / SVIP 等会员权益，下拉中会出现「仅 VIP 可见」「仅 SVIP 可见」等选项，可手动指定 |
| 默认播放器 | 该剧默认走的线路，默认 rrys。如配置了多条线路可手动切换 |
| 列表排序 | 数字越小越靠前 |

**关于"跟随频道配置"和"手动指定"的区别**：

| 情况 | 含义 |
|------|------|
| 选择「跟随频道配置」 | 此剧的付费状态跟随所属频道。如果之后频道改为收费，此剧也会自动变成收费 |
| 选择「仅 VIP 可见」/「仅 SVIP 可见」 | 此剧的付费状态被你手动锁定。即使之后频道改了，此剧也保持你设置的状态不变 |

> 💡 选「跟随频道配置」时，下方会显示 🟢 绿色提示"跟随型：频道收费变更时会被联动刷新"。
> 选「仅 VIP / SVIP 可见」时，下方会显示 🟠 橙色提示"手动型：不会被联动刷新覆盖"。
> 看到这两个提示即可知道该剧的付费状态是否会被频道变更影响。

---

### 6.4 常见运营场景

| 场景 | 操作步骤 |
|------|---------|
| 新剧入库后想推一波 | 编辑 → 标签加「新上线」+「推荐」→ 投放与高级开 Banner，配置首页第 1~3 位 |
| 调整某集的播放源质量 | 编辑 → 播放源管理 → 在对应集后追加 `$其他线路标识` |
| 限时 VIP 专享 | 编辑 → 计费模式选「仅 VIP 可见」→ 保存后即使频道改成免费也不会被刷掉 |
| 把剧放到追剧日历 | 编辑 → 更新计划 → 开「展示在追剧日历」→ 选更新日和时间 → 保存 |
| 别名搜索命中 | 编辑 → 搜索别名追加多个常见叫法 |
| 调整剧的冷启动数据 | 编辑 → 剧集状态 → 互动数据 → 填初始收藏 / 点赞次数（播放次数仅用于内部记录） |

---

## 七、运营策略（Banner 推荐管理 / 更新计划管理）

> 💡 视频管理负责"把每一部剧加工好"，运营策略负责"把这些剧编排到 App 的展示位上"。
> 同一份数据有**两个配置入口**，互相同步：
> - **快捷配置**：在影视库管理编辑某部剧时顺手设置（适合处理单部剧）
> - **聚合配置**：在 Banner 推荐管理 / 更新计划管理两个独立页面集中查看和编排（适合统筹整个 App）
>
> 无论从哪个入口改，最终都呈现在 App 侧。两边看到的是同一份数据。

### 7.1 两个入口的关系

| 入口 | 在哪里 | 适合什么 | 操作粒度 |
|------|--------|---------|---------|
| **快捷配置** | 影视库管理 → 编辑某部剧 → 投放与高级 / 更新计划 | 录入或调整单部剧时顺手设置 | 单部剧 |
| **聚合配置** | 推荐管理 → Banner 推荐管理 / 更新计划管理 | 集中查看全部条目、统一编排、批量管理 | 跨剧、全局 |

> ⚠️ 在剧里开了 Banner，Banner 推荐管理列表里就能看到这条；在剧里开了追剧日历，更新计划管理里就能看到这条。反向在聚合页里编辑，剧的编辑弹窗也会同步。**不存在"两份数据"，只是两个看同一份数据的窗口。**

---

### 步骤 10 · Banner 推荐管理

**入口**：推荐管理 → Banner 推荐管理

管理 App 里的轮播图。从某部剧开出来的 Banner、纯运营的活动位，都汇总到这里。

**这页能做什么**

- 新增一条 Banner
- 调整已有 Banner 的位置 / 展示时间 / 上下线
- 核对 Banner 在 App 的真实效果

**新增一条 Banner**

- 推广某部剧：关联该剧 + 链接类型选「视频详情」，用户点击跳该剧播放页
- 做活动位：不关联剧，自己填标题 + 传配图 + 选其他链接类型指向活动页
- 最后选好投放位置（首页 / 分类页）和展示时间段

**控制上下线（两个条件都满足才展示）**

- 「状态」开关为上线
- 当前时间落在「展示时间」段内
- 👉 想自动上下线：设好展示时间段；想立刻撤下：直接关状态开关，不用改时间

**核对效果**

- 工具栏「首页预览」「分类页预览」，直接看 App 实际展示样子

> 💡 **和影视库快捷配置同一份数据**：剧里开了 Banner 选了首页第 2 位，这条就出现在这里；在这里改位置或时间，剧的编辑弹窗也跟着变。

---

### 步骤 11 · 更新计划管理

**入口**：推荐管理 → 更新计划管理

管理 App 端「追剧日历」给用户看的更新预告。

> ⚠️ **只是给用户看的预告，不是实际更新时间。** 这里设的"周五 21:00"只是告诉用户大概什么时候来看新集；剧真正多出新集由采集任务决定，跟这里无关。

**一条更新计划怎么配**

- 必须关联一部剧（视频名称必填）
- 选更新日：周一 ~ 周日，可多选
- 设更新时间
- 同一天多部剧：用「排序」决定先后（数字越小越靠前）
- 不想展示了：关「状态」开关，不必删除
- 只在某段时间展示：用「有效期」限定，默认「不受限」=一直展示

**大部分不用手动建**

- 剧里开了「展示在追剧日历」开关，系统自动生成对应条目
- 来这页主要是集中查看和微调

> ⚠️ 视频名称必填。列表里出现视频名称为空的，是异常数据，联系技术处理。

> 💡 **和影视库快捷配置同一份数据**：剧里开了追剧日历选了周五周六，这条就出现在这里；在这里从周五挪到周三，剧的编辑弹窗也跟着变。

---

### 7.2 运营策略 · 常见场景

| 场景 | 推荐入口 | 操作 |
|------|---------|------|
| 给一部新剧上首页 Banner | 快捷配置 | 影视库编辑该剧 → 投放与高级 → 开 Banner → 选首页 + 位次 |
| 集中管理 / 查看所有 Banner | 聚合配置 | Banner 推荐管理 → 列表查看、编辑、用状态开关与展示时间控制上下线 |
| 做一个限时活动 Banner | 聚合配置 | Banner 推荐管理 → 新增轮播图 → 配图 + 标题 + 选链接类型 + 设展示时间段 |
| 核对 Banner 在 App 的效果 | 聚合配置 | Banner 推荐管理 → 首页预览 / 分类页预览 |
| 立刻撤下某个 Banner | 聚合配置 | Banner 推荐管理 → 关掉该条的状态开关 |
| 单部剧设置追剧更新预告 | 快捷配置 | 影视库编辑该剧 → 更新计划 → 开开关 → 选更新日和时间 |
| 集中查看 / 调整所有更新预告 | 聚合配置 | 更新计划管理 → 列表查看、改更新日/时间/排序、启停 |
| 临时停掉某条更新预告 | 聚合配置 | 更新计划管理 → 关掉该条的「状态」开关 |

---

## 八、新人接入 Checklist

第一次配置一个新 App 时，按以下顺序操作：

**第一阶段 · 前置准备**
- [ ] 配置视频分类（至少 1 个顶级 + 若干子分类）
- [ ] 配置播放线路（最简路径：填好系统预置 rrys 线路的 AppID/AppKey）
- [ ] （可选）如需自定义 Lua 解析，先配置 Lua 脚本组
- [ ] （可选）新建自定义线路并绑定脚本组
- [ ] 测试每条线路的连通性

**第二阶段 · 全局策略**
- [ ] 配置全局策略 → 判重规则
- [ ] 配置全局策略 → 默认更新策略
- [ ] 配置全局策略 → 播放线路合并
- [ ] 配置全局策略 → 全局安全过滤

**第三阶段 · 采集源接入**
- [ ] 添加采集源（基础配置 + 测试连通 ✅）
- [ ] 暂时关闭自动采集，先手动测试
- [ ] 进入采集源浏览页，**绑定所有远程分类**到本地频道
- [ ] 手动触发一次「采当天」采集
- [ ] 在采集日志确认任务成功（失败数为 0 或可接受）
- [ ] 在视频管理确认视频入库
- [ ] 在 App 端确认能播放
- [ ] 回到采集源编辑，**开启自动采集**（设定合适频率）

**第四阶段 · 视频加工与投放（视频管理）**
- [ ] 在视频管理列表确认采集入库的视频归属正确（频道/分类、地区、年份）
- [ ] 对重点剧目编辑：完善简介、封面、标签（推荐 / 新上线 等）
- [ ] 配置搜索别名，确保 App 端常见叫法都能搜到
- [ ] 调整冷启动收藏 / 点赞次数（播放次数仅用于内部记录）
- [ ] 如需推广，开 Banner 投放并选定页面与位次
- [ ] 如为追剧型剧目，开"展示在追剧日历"并配置更新日/时间
- [ ] 检查计费模式：默认跟随频道，特殊剧可手动指定"仅 VIP / SVIP 可见"

**第五阶段 · 运营策略编排（Banner / 更新计划）**
- [ ] 进入 Banner 推荐管理，检查 / 编辑各 Banner 的位置、链接类型、展示时间
- [ ] 用「首页预览 / 分类页预览」核对 Banner 在 App 的真实效果
- [ ] 如有活动，新增轮播图（配图 + 标题 + 链接类型 + 展示时间段）
- [ ] 进入更新计划管理，检查全部更新计划的更新日 / 时间 / 排序
- [ ] 确认需要展示的更新计划「状态」为启用、在有效期内

**第六阶段 · 扩展**
- [ ] 重复第三阶段步骤接入更多采集源
- [ ] 观察 1-2 天采集日志，调整频率/屏蔽词

---

## 附录 · 字段对照速查

| 远程数据字段 | 本地字段 | 是否参与判重 | 是否默认更新 |
|------------|---------|------------|------------|
| vod_name | 影片名称 | ✓（必选） | ✗ |
| vod_year | 上映年份 | ☐（可选） | ✗ |
| type_id | 远程分类 | ☐（可选） | ✗（需通过分类映射） |
| vod_director | 导演 | ☐（可选） | ✗ |
| vod_play_url | 播放资源分组 | ✗ | ✓ |
| vod_remarks | 影片备注（如"更新至第12集"）| ✗ | ✓ |
| vod_total | 集数进度 | ✗ | ✓ |
| vod_pic | 影片海报封面 | ✗ | ✗（保护本地修改）|
| vod_content | 简介 | ✗ | ✗ |
| vod_score | 豆瓣评分 | ✗ | ✗ |
