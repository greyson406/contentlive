# 员工与角色管理操作手册

> 适用页面：组织架构 → 角色管理 / 员工管理
> 适用对象：后台管理员
> 阅读方式：本手册按"新员工接入"的完整链路组织——**先建角色（决定能做什么），再建员工（把人和角色绑定），最后通知登录**。

---

## 一、整体流程

新员工要登录系统使用功能，需要依次完成三步：

```
第 1 步：建角色          第 2 步：建员工          第 3 步：通知登录
（角色管理）     →      （员工管理）     →      （分享登录地址）
决定能做什么            把人和角色绑定           让员工进来
```

---

## 二、第一步：创建角色

### 2.1 操作入口

左侧菜单 → 组织架构 → 角色管理 → 点击「+ 新建角色」。

### 2.2 系统自带角色

打开页面会看到两个系统自带的角色，**均不可删除**：

| 角色 | 权限范围 |
| --- | --- |
| 管理员（admin） | 拥有所有权限，可管理所有 App 的数据 |
| 只读成员（readonly） | 仅可查看，不能修改 |

如果这两个角色不满足需要（例如想要一个"只管视频运营"的角色），再自行新建。

### 2.3 填写新角色

点击「新建角色」后，右侧滑出表单，分为 **基本信息** 和 **权限分配** 两个 Tab。

**基本信息 Tab**

| 字段 | 说明 |
| --- | --- |
| 角色名称 | 角色的显示名，如"视频运营"。创建后可修改。 |
| 角色标识 | 类似英文代号，如 `video_ops`。**创建后不可修改，请确认后再填**。 |
| 启用状态 | 默认"启用"。关闭后该角色暂时不可用。 |
| 默认角色 | 重要开关，详见下方说明。 |
| 角色介绍 | 可选，简要描述角色用途。 |

> 🔑 **关于「默认角色」**
> 将某个角色设为"默认角色"后，**所有新创建的员工**（无论直接创建还是邀请绑定），若未单独配置角色，都会**自动套用该默认角色的权限**。
> 适用场景：团队大部分新人岗位相同时，设一个默认角色可省去重复配置。不需要时保持"否（需手动分配）"即可。

**App 数据权限**（表单下方）

决定该角色的员工能查看哪些 App 的业务数据：

- **全部 App**：可查看所有 App 的数据。
- **指定 App**：仅可查看勾选的 App 的数据。

**权限分配 Tab**

切换到顶部「权限分配」Tab，勾选该角色具体可使用的功能（如内容审核、用户管理等）。

### 2.4 保存

点击右下角「确认」，角色创建成功。

---

## 三、第二步：创建员工

### 3.1 操作入口

左侧菜单 → 组织架构 → 员工管理。

进入页面后，顶部蓝色横幅中会显示一个 **子账号登录地址**，例如：

```
https://contentlive.example.com/#/auth/login
```

> 📌 该地址会一直显示在此处，无需特意复制保存，需要时直接发给员工即可。

### 3.2 两种创建方式

**方式 A：直接创建（管理员代建账号）**

适合：已有员工邮箱，希望直接开通账号并设置密码。

操作步骤：

1. 点击「+ 创建员工」。
2. 在表单中填写：

| 字段 | 填写说明 |
| --- | --- |
| 邮箱地址（必填） | 员工邮箱，即其登录账号 |
| 初始密码（必填） | 至少 6 位，建议员工首次登录后自行修改 |
| 分配角色 | 选择已建好的角色（可多选，权限取并集）。**不选则自动套用默认角色权限**。 |
| App 数据权限 | 全部 App / 指定 App，决定可查看的业务数据范围 |

3. 点击「确认」，员工账号创建成功。

**方式 B：邀请绑定（员工自行注册）**

适合：不代设密码，让员工通过邮件自行设置并注册。

操作步骤：

1. 点击「邀请/绑定」。
2. 在表单中填写：

| 字段 | 填写说明 |
| --- | --- |
| 邮箱地址（必填） | 员工邮箱，系统将向此邮箱发送邀请链接 |
| 分配角色 | 可多选；**不选则自动套用默认角色权限**。 |
| App 数据权限 | 全部 App / 指定 App，决定可查看的业务数据范围 |

3. 点击「确认」，系统发送邀请邮件。
4. 员工收到邮件后点击链接，自行设置密码并加入工作区。
5. 可在「邀请记录」Tab 查看邀请的发送与接受状态。

> 💡 **两种方式如何选择**
> · **直接创建**：账号即建即用，适合急需上手或批量开号。
> · **邀请绑定**：员工自设密码更安全，适合正式场景。

---

## 四、第三步：通知员工登录

> 仅 **直接创建** 方式需要此步骤。邀请绑定方式下员工会直接收到邮件，按邮件指引操作即可。

将以下两项发给员工：

- **登录地址**：员工管理页顶部的 `https://...auth/login` 链接（一直显示在此处）。
- **账号密码**：邮箱 + 设置的初始密码。

员工打开链接 → 输入账号密码 → 登录成功，即可开始使用。

---

## 五、完整示例

**场景**：为小王开通账号，负责视频运营。

1. 进入「角色管理」→ 新建角色：
   - 名称：视频运营
   - 标识：`video_ops`
   - 默认角色：否
   - App 数据权限：指定 App → 勾选「contentlive」
   - 权限分配 Tab：勾选视频管理、内容审核相关权限
   - 点击「确认」
2. 进入「员工管理」，记下顶部的登录地址。
3. 点击「+ 创建员工」：
   - 邮箱：`xiaowang@company.com`
   - 初始密码：`abc123456`
   - 分配角色：勾选「视频运营」
   - App 数据权限：全部 App（或与角色保持一致）
   - 点击「确认」
4. 将登录地址、邮箱、密码发给小王，小王登录后即可使用。

---

## 六、常见问题

**Q1：员工分配了多个角色，权限如何计算？**
取并集。任意一个角色拥有的权限，员工都拥有。

**Q2：员工自身的 App 权限与角色的 App 权限不一致怎么办？**
同样取并集。两者合并后的范围即为员工可访问的范围。

**Q3：忘记给员工分配角色会怎样？**
无论直接创建还是邀请绑定，都会**自动套用「默认角色」的权限**。因此建议至少配置一个默认角色，避免新员工无任何权限。

**Q4：未设置「默认角色」，又忘记给员工分配角色呢？**
员工只能获得最基础的访问权限（基本等于登录后无法操作），建议尽快补全角色配置。

**Q5：角色标识填错了能改吗？**
不能。只能删除角色后重新创建，因此创建前请确认。

**Q6：邀请邮件员工未收到怎么办？**
在「邀请记录」Tab 查看发送状态，提醒员工检查垃圾邮件，或重新发起邀请。
