# 使用说明（如何打开这个网站）

这是一个基于 [Docsify](https://docsify.js.org/) 的文档网站。所有依赖（docsify 核心 + 插件 + 代码高亮）都已打包在 `vendor/` 目录里，**完全离线可用，无需联网**。

---

## 方式一：本地预览（推荐）

由于浏览器的安全策略，直接双击 `index.html` 用 `file://` 打开时，Markdown 文件可能加载不出来。需要起一个本地静态服务器：

**用 Python（系统一般自带）**

在本文件夹下打开终端，执行：

```bash
# Python 3
python -m http.server 3000
```

然后浏览器访问 `http://localhost:3000`。

**用 Node.js（如果装了 docsify-cli）**

```bash
npm i docsify-cli -g
docsify serve .
```

默认访问 `http://localhost:3000`。

---

## 方式二：部署到服务器 / 静态托管

整个文件夹是纯静态资源，可直接上传到任意静态托管：

- **Nginx / Apache**：把整个文件夹作为站点根目录即可。
- **GitHub Pages / Gitee Pages / Netlify / Vercel**：直接把文件夹推上去。文件夹里已包含 `.nojekyll`，确保 `_sidebar.md` 等下划线开头的文件不被忽略。

Nginx 参考配置：

```nginx
server {
    listen 80;
    server_name docs.example.com;
    root /var/www/contentlabs-docs;   # 指向本文件夹
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

---

## 站点结构

| 文件 / 目录 | 作用 |
| --- | --- |
| `index.html` | 站点入口，含主题与插件配置 |
| `README.md` | 首页内容 |
| `_sidebar.md` | 左侧导航目录 |
| `_navbar.md` | 顶部导航栏 |
| `01~06-*.md` | 六份操作手册正文 |
| `vendor/` | docsify 核心、插件、代码高亮（离线依赖） |
| `.nojekyll` | 部署到 GitHub Pages 时必需 |

---

## 如何修改内容

直接编辑对应的 `.md` 文件即可，刷新网页就能看到更新，**无需重新构建**。

- 改某份手册 → 编辑对应的 `0X-xxx.md`
- 改首页 → 编辑 `README.md`
- 改左侧目录 → 编辑 `_sidebar.md`
- 增减手册 → 把新 `.md` 放进文件夹，并在 `_sidebar.md`、`_navbar.md`、`README.md` 里加上链接
