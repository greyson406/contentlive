# 内容运营管理操作手册（UGC 管理）

> 适用范围：敏感词审核、弹幕管理、评论管理、举报中心、用户反馈
> 适用对象：运营 / 审核人员
> 阅读方式：本手册说明 UGC（用户生成内容）治理各模块的功能与操作，覆盖从事前过滤到事中管理再到事后处置的完整闭环。

---

## 一、总览

内容运营管理聚焦平台 **UGC（用户生成内容）** 的全链路治理，覆盖「事前过滤 → 事中管理 → 事后处置」的完整闭环，由五个模块构成：

| 模块 | 治理阶段 | 核心作用 |
| --- | --- | --- |
| 敏感词审核 | 事前 | 在内容入库前进行词库匹配与拦截 |
| 弹幕管理 | 事中 | 查看、检索、处置全量弹幕 |
| 评论管理 | 事中 | 查看、检索、处置评论及关联回复树 |
| 举报中心 | 事后 | 处理 App 用户举报的违规弹幕 / 评论 |
| 用户反馈 | 事后 | 接收并处理 App 用户的问题反馈与建议 |

治理对象贯穿 **弹幕、评论、昵称、反馈** 四类文本，所有内容在入库前均会经过统一的敏感词审核层。

### 模块协同关系

```
              ┌─────────────────────────────┐
   用户发送 →  │   敏感词审核（事前 · 统一）   │
              │   弹幕 / 评论 / 昵称 / 反馈    │
              └──────────────┬──────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          ▼                  ▼                  ▼
      拒绝入库            内容脱敏(*)          正常入库
   （App 不展示）     （App 打码展示）       （App 正常展示）
                             │
              ┌──────────────┴──────────────┐
              ▼                             ▼
        弹幕管理 / 评论管理（事中查看与处置）
              │
   ┌──────────┴──────────┐
   ▼                     ▼
举报中心（事后）       用户反馈（事后）
（违规内容处置）     （问题沟通与闭环）
```

---

## 二、敏感词审核

敏感词审核是 UGC 治理的第一道防线，所有用户产生的文本内容在落库前都会先经过词库匹配。

### 2.1 词库管理

支持 **自建词库** 与 **自定义导入词库** 两种方式，词库以「分类」为单位进行组织和管理。

词库分类卡片提供以下能力：

- **启停开关**：可整体启用 / 停用某个词库分类，停用后该词库不参与匹配。
- **拼音匹配开关**（拼）：开启后支持对拼音 / 变体形式进行识别，提升变形规避词的识别率。
- **编辑 / 删除**：维护词库分类的基础信息。

词库内的敏感词管理支持：

- **新增敏感词**：单条手动录入。
- **批量导入**：一次性导入大批量词条。
- **全局测试 / 全局检索**：跨词库验证某段文本是否命中，或检索某个词存在于哪些词库。
- **词库内搜索**：在当前词库范围内快速查找词条。

每个敏感词记录包含「敏感词、来源、状态（生效 / 停用）、创建时间」等字段，并支持单条移除。

### 2.2 适用范围

同一套词库会统一作用于以下四类内容的审核：

```
弹幕  ·  评论  ·  昵称  ·  意见反馈
```

这意味着维护一套词库即可覆盖全部 UGC 入口，无需为每个入口单独配置。

### 2.3 处理类型（两种策略）

词库整体支持配置为 **两种处理类型**，分别对应 App 端的两种处理方式。两种类型有一个共同前提：

> 📌 **用户侧无感**：无论命中哪种策略，发送内容的用户本人在客户端看到的都是「发送成功、内容正常」的状态，不会感知到被拦截或被处理。

在此前提下，发送完成后的实际处理分为两类：

| 类型 | 命中后行为 | App 端最终表现 | 适用场景 |
| --- | --- | --- | --- |
| **类型一 · 拒绝入库** | 内容直接不入库 | 该内容不会在 App 中向其他用户展示 | 严重违规、引流、广告等需彻底屏蔽的内容 |
| **类型二 · 内容脱敏** | 内容正常入库，敏感部分以 `*` 替换 | 在 App 中以「打码」形式展示（敏感字被星号遮蔽） | 轻度敏感、可保留但需遮蔽的内容 |

> 💡 在弹幕 / 评论管理列表中，被脱敏处理的内容会以「**内容脱敏**」状态进行标识，便于运营识别。

---

## 三、弹幕管理

弹幕管理页用于查看和处置平台内全量弹幕，可定位到具体「视频 → 线路 → 集数 → 时间点」维度。

### 3.1 检索条件

支持多维组合检索：

- **视频**：按视频标题搜索
- **线路**：按来源线路筛选
- **集数**：按集数定位
- **内容**：搜索弹幕文本
- **发送人**：按邮箱 / 昵称 / 用户 ID 搜索
- **状态**：按弹幕状态筛选
- **开始（秒）/ 结束（秒）**：按弹幕出现的时间点区间筛选

### 3.2 列表字段

| 字段 | 说明 |
| --- | --- |
| 视频 | 弹幕所属视频（标题 + 视频 ID） |
| 内容 | 弹幕文本内容 |
| 时间点 | 弹幕在视频中出现的时间（如 3:06） |
| 发送人 | 发送者昵称 / 邮箱 / 用户 ID，VIP 用户带 `VIP` 标识 |
| 来源线路 | 弹幕来源线路（如 mtm3u8） |
| 模式 | 弹幕展示模式（顶部 / 滚动 等） |
| 举报 | 该弹幕累计被举报次数 |
| 状态 | 弹幕当前状态（已公开 / 内容脱敏 等） |
| 发送时间 | 弹幕实际发送时间 |

### 3.3 操作能力

**列表级操作**

- **新建弹幕**：手动创建弹幕。
- **弹幕密度分析**：分析弹幕在时间轴上的分布密度。
- **清理无效弹幕**：批量清理无效数据。

**单条操作（编辑 + 更多）**

「更多」下拉提供以下处置动作：

| 动作 | 说明 |
| --- | --- |
| 通过 | 审核通过，正常展示 |
| 屏蔽 | 屏蔽该条弹幕 |
| 规则屏蔽 | 按规则进行屏蔽处理 |
| 禁言 | 对发送者执行禁言 |
| 冻结 | 冻结该条弹幕 / 相关账号 |
| 删除 | 删除该条弹幕 |

---

## 四、评论管理

评论管理用于查看和处置评论，并支持查看评论之间的关联（回复树）关系。例如对一条「一级评论」，可展开查看其他用户对它的回复。

### 4.1 评论列表

检索条件包括：视频、集数、用户 ID、关键词、状态。

列表字段（两种视图）：

- **发布者视图**：ID、发布者（昵称 / 邮箱 / 用户 ID，VIP 标识）、评论内容、操作。
- **视频视图**：视频（标题 + ID）、互动（点赞数 / 回复数）、状态（显示开关）、IP、发布时间、操作。

单条操作支持 **置顶 / 取消置顶**、**删除**；列表级支持 **发布评论**、**一键清理无效内容**、**导出**。

> 💡 评论支持层级结构：一级评论可展开查看其下的全部回复，便于运营在完整语境中判断和处置。

### 4.2 回收站

被删除的评论进入「回收站」，不会立即物理销毁，提供二次处置缓冲：

- **关键词搜索**：在回收站内检索。
- **恢复**：将误删评论还原至列表。
- **彻底删除**：物理删除，不可恢复。

回收站中保留发布者、评论内容、回复标识等信息，便于核对后再决定恢复或销毁。

### 4.3 删除规则（高级配置）

#### 这一节在解决什么问题？

评论不是一条条孤立的，而是 **有层级的对话**。比如下面这串评论：

```
A: 11          ← A 发了一条评论
  └ B: 22      ← B 回复了 A（引用 11）
      └ A: 33  ← A 又回复了 B（引用 22）
```

现在运营要删掉中间那条 **B:22**（比如它是违规内容）。问题来了：**挂在 B:22 下面的 A:33 怎么办？**

- 如果连 A:33 一起删，A 的话就被"误伤"了；
- 如果只删 B:22 不管 A:33，那 A:33 这条"回复 B 的话"就会变得没头没尾，读的人看不懂在回复谁。

「删除规则」就是用来 **统一规定这种情况下子回复如何处理** 的。设定一次，之后所有删除操作都按这个规则自动执行，运营不用每次手动判断。

#### 基础开关

在选择删除策略之前，有两个全局开关：

- **评论开关**：总开关。关闭后 App 用户将无法发表任何评论。
- **显示模式**：决定客户端如何聚合评论。
  - **按整部剧**：客户端显示该剧所有集的评论（混在一起）。
  - **按每集**：每一集只显示自己那一集的评论。

#### 四种删除方案对比

仍用上面 `A:11 → B:22 → A:33` 这串，**目标是删掉 B:22**，看四种方案分别会变成什么样：

| 方案 | 通俗解释 | 删掉 B:22 后会变成 | 优缺点 |
| --- | --- | --- | --- |
| **方案一 · 占位保留** | 不真删，只把内容换成"此评论已删除"，坑位还在 | A:11 → `[内容已删除]` → A:33（结构原样保留） | ✅ 语境最完整，读者知道这里曾有过对话（类似 Reddit）<br>❌ 列表里会留下一堆"已删除"占位 |
| **方案二 · 子节点上浮** | 删掉 B:22，把它下面的 A:33"提"上来，直接挂到 A:11 下面 | A:11 → A:33（A:33 上移一层） | ✅ 列表干净，没有占位<br>❌ A:33 本来是"回复 B"的，现在挂在 A 下面，语境断裂、容易看不懂 |
| **方案三 · 级联隐藏** | 删掉 B:22，连同它下面的所有回复一起隐藏 | A:11（22 和 33 全部消失） | ✅ 最彻底，适合清理广告、刷屏、整串违规<br>❌ 会"连坐"无辜的子回复 |
| **方案四 · 精细化递归（推荐）** | 不一刀切，根据"这串对话有多深、有多少条回复"自动选上面几种做法 | 视具体情况而定（见下） | ✅ 在"保留讨论"和"彻底清理"之间自动平衡，大规模评论区下最省心 |

#### 方案四是怎么"自动判断"的？

方案四不需要每次选，而是按两个指标自动决定怎么删：**这串回复有多深（深度）**、**这串回复下面挂了多少条（回复数）**。规则如下：

| 条件 | 自动处理 | 含义 |
| --- | --- | --- |
| 深度 ≤ 3 层 且 回复数 ≤ 50 条 | **打标隐藏** | 这是一小串对话，直接隐藏掉最干净 |
| 深度 ≤ 3 层 且 回复数 > 50 条 | **打标删除（保留结构）** | 回复很多，全隐藏会丢失大量讨论，所以只删目标、保留结构 |
| 深度 > 3 层（无论回复数） | **保留不动** | 已经是很深的楼中楼，动它会破坏太多语境，干脆不碰 |

> 💡 一句话理解：**小范围的就直接隐藏，讨论量大的就只删目标保留楼层，太深的就不动**——尽量少误伤，又能清掉问题内容。

#### 方案四参数配置

上面规则里的"3 层""50 条"不是写死的，可自行调整：

- **递归最大深度**：删除时最多往下处理几层（可填 1–10），超过此深度的子评论一律不受影响。默认 `3`。
- **回复数阈值**：某串回复的总数 **≤ 该值** 就打标隐藏；**> 该值** 就只打标删除并保留楼层结构（可填 1–10000）。默认 `50`。

> ⚠️ 更改方案或参数后，**历史上已删除评论** 的展示状态会按新规则实时重新计算并同步生效。

---

## 五、举报中心

举报中心对应 App 用户对违规内容发起的举报，目前以 **弹幕** 为处置单位，按内容维度累计被举报次数，便于运营快速集中处置。

### 5.1 处置单位与逻辑

- 以 **评论 / 弹幕为单位** 聚合举报，而非按每次举报独立处理。
- 系统自动计算每条内容的 **累计被举报次数**，可据此设置优先处理顺序（如高举报量优先）。

### 5.2 列表字段

| 字段 | 说明 |
| --- | --- |
| 举报 ID | 举报记录唯一标识 |
| 弹幕内容 | 被举报的弹幕文本 |
| 弹幕池 | 该弹幕所属的弹幕池 |
| 举报原因 | 用户提交的举报理由 |
| 状态 | 举报处理状态 |
| 弹幕状态 | 被举报弹幕的当前状态 |
| 累计举报 | 该内容被举报的总次数 |
| 举报时间 | 举报发起时间 |

支持按「状态」筛选，便于运营快速过滤待处理项并执行处置。

---

## 六、用户反馈

用户反馈用于接收 App 用户对相关问题的反馈与建议，并支持以 **类似微信对话** 的形式与用户直接沟通、持续跟进处理。

### 6.1 概览指标

页面顶部提供三个核心指标卡片：

- **待处理故障 / 建议**：当前待处理的反馈数量。
- **当前视图闭环率**：当前筛选范围内反馈的闭环（已解决）比例。
- **大盘累计反馈总量**：全平台累计反馈总数。

### 6.2 检索条件

- **所属用户 ID**：精确搜索。
- **指定硬件指纹（设备 ID）**：精确搜索。
- **设备机型模糊搜索**：如 Xiaomi 等。
- **问题预设分类**：按分类筛选。
- **实时流转状态**：按处理状态筛选。

### 6.3 列表字段与处理方式

列表包含：工单 ID、类型、反馈摘要、所属用户、环境 / 设备、操作。

处理特性：

- **对话式沟通**：以类似微信聊天的形式与用户一对一沟通，持续跟踪问题直至闭环。
- **导出**：支持导出当前反馈（CSV）。
- **后续规划**：将逐步支持 **图片上传** 等更丰富的沟通能力。

---

## 附：术语表

| 术语 | 含义 |
| --- | --- |
| UGC | 用户生成内容（弹幕、评论、昵称、反馈等） |
| 内容脱敏 | 命中敏感词后，将敏感部分以 `*` 替换并正常入库展示 |
| 拒绝入库 | 命中敏感词后内容不落库，App 不向他人展示 |
| 用户侧无感 | 触发处理时，发送者本人在客户端仍看到「发送成功」 |
| 占位保留 / 子节点上浮 / 级联隐藏 / 精细化递归 | 评论删除时对子回复的四种处置策略 |
| 闭环率 | 反馈中已解决（完成处理）的占比 |
| 弹幕池 | 弹幕的归属集合 |
